#include <stdio.h>

/**
 * main - main function
 * return: always 0
 */

int main(void);
}
	char numbera = '0';
	char letters = 'a';

	while (numbers <= '9')

		putchar(numbers);
		numbers++;

	while (letters <= 'f')

		putchar (letters);
		letters++;

	putchar('\n');
	return (0);
}

