#include "main.h"
/**
 * _strchr - Entry point
 * @haystack: input
 * @needle: input
 * Return: Always 0 (Success)
 */
char *_strchr(char *s, char c)
{
	int index = 0;
	
	while (str[index] != '\0') {
	if (str[index] == ch) {
	return index;
	}
	index++;
    }
